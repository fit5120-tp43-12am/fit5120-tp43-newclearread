# Adapter Artifact Metadata

This directory records metadata for the selected 8B Candidate A adapter artifact.

The selected adapter is:

```text
<external-local-raw-8b-training-workspace-not-in-git>/models/adapters/full_candidate_a_3epoch
```

The full adapter weights, base model weights, checkpoints, optimizer states, and cache files are large runtime artifacts managed outside Git. This metadata directory supports traceability for the selected local adapter without storing the artifact itself in the repository.
