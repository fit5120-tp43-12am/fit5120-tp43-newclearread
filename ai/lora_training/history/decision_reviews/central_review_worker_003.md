# Central Review: Worker 003 WSL Environment Setup

Date: 2026-04-28

## Reviewed Inputs

- Worker return summary from the central-brain chat.
- `ai/lora_training/reports/WSL_ENVIRONMENT_REPORT.md`
- `ai/lora_training/history/decision_reviews/worker_003_wsl_environment_setup.md`
- `ai/lora_training/scripts/check_wsl_training_env.py`
- `ai/lora_training/scripts/check_wsl_training_env.sh`
- Team repo status under `<team-repo-root>`

## Central Verification

- Reran `scripts/check_wsl_training_env.sh --hf-check` in WSL from the training workspace.
- Environment check returned `overall_ok: true`.
- Conda env is active as `clearread-llama-lora`.
- Python executable is `<conda-env-path>/clearread-llama-lora/bin/python`.
- Python version is `3.11.15`.
- `torch 2.10.0+cu128` imports successfully.
- `torch.version.cuda` is `12.8`.
- `torch.cuda.is_available()` is `true`.
- CUDA device count is `1`.
- GPU is `NVIDIA GeForce RTX 4070 Ti SUPER`.
- GPU total VRAM is about `16375.5 MiB`.
- BF16 support is `true`.
- CUDA tensor test passed.
- Required imports passed: `torch`, `transformers`, `datasets`, `accelerate`, `peft`, `trl`, `bitsandbytes`, `unsloth`, `huggingface_hub`, `safetensors`, and `sentencepiece`.
- Fresh-process `unsloth` import before `transformers` passed.
- Hugging Face CLI exists but `hf auth whoami` reports not logged in.
- Metadata-only model checks succeeded for:
  - `unsloth/Llama-3.1-8B-Instruct-unsloth-bnb-4bit`
  - `unsloth/Llama-3.1-8B-Instruct-bnb-4bit`
  - `meta-llama/Meta-Llama-3.1-8B-Instruct`
- Official Meta model metadata reports `gated = manual`.
- Old WSL env `<previous-conda-env-path>/dyslexia-summary` still exists.
- Old WSL project `<previous-project-path>` still exists.
- No `.safetensors`, `.pt`, `.pth`, or `.bin` files were found under the training workspace.
- Local `unsloth_compiled_cache` files were generated by import checks; this cache is expected and ignored by Git.
- Secret scan of environment/report/log files found only false positives such as `tokenizers`, `hf_transfer`, and explicit "No token" report text; no token value was found.

## Git Verification

- Team repo branch is `feature/ai-llama-lora-training`.
- Local HEAD equals upstream at `a2fc6bea152b6ea35594604d132801eed07c8abf`.
- Worktree is clean.
- No `ai/**/*.jsonl` files are tracked.
- No checked model binary patterns are tracked.
- Representative cache/model ignore rules are active.

## Notes

- Hugging Face CLI login is still needed later if the project uses the official gated Meta fallback model. The public Unsloth model metadata is accessible without login.
- Flash Attention 2 appears broken in this environment, and Unsloth falls back to xformers. This is not a blocker for smoke preparation because imports, CUDA, and xformers availability passed.
- Unsloth's resolver upgraded the stack to `torch 2.10.0+cu128`; runtime CUDA checks passed, so this is accepted for now.

## Decision

Worker 003 passes central-brain review with notes.

The next approved work unit is Worker 004, smoke training scripts/configs and assistant-only loss mask checks, using:

```text
ai/lora_training/history/work_orders/004_prepare_smoke_training.md
```
