# Content Scripts

This folder is reserved for future page-level reading support features.

Phase 1 does not register any content scripts in `manifest.json`, so the extension does not read or modify webpages yet.
